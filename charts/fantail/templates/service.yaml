apiVersion: v1
kind: Service
metadata:
  name: {{ include "fantail.fullname" . }}
  labels:
    {{- include "fantail.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fantail.selectorLabels" . | nindent 4 }}
