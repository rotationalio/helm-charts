Thank you for installing {{ .Chart.Name | title }}.

{{ if .Values.ingress.enabled -}}
You can reach the Fantail service via the following endpoint:

- URL: https://{{ .Values.ingress.hostname }}
{{- end }}

Your release is named {{ .Release.Name }} and your application is defined as:

- application name: {{ include "fantail.name" . }}
- service name: {{ include "fantail.fullname" . }}

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

Labels added to all elements of the release are as follows:

{{ include "fantail.labels" . | nindent 4 }}