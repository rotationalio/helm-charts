apiVersion: v1
kind: Service
metadata:
  name: {{ include "honu.fullname" . }}
  labels:
    {{- include "honu.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "honu.selectorLabels" . | nindent 4 }}
