apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "parlance.fullname" . }}-test-connection"
  labels:
    {{- include "parlance.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "parlance.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
