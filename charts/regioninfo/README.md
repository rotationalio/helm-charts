# Region-Info

Creates a region-info config map that can be used to populate region information for compliance and provenance information into pods being deployed.

This can either be used as a dependency in another chart or can be deployed as a standalone chart for inclusion into other pods.