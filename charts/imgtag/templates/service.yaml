apiVersion: v1
kind: Service
metadata:
  name: {{ include "imgtag.fullname" . }}
  namespace: {{ .Release.namespace }}
  labels:
    {{- include "imgtag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "imgtag.selectorLabels" . | nindent 4 }}
