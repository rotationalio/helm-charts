apiVersion: v1
kind: Service
metadata:
  name: {{ include "quarterdeck.fullname" . }}
  labels:
    {{- include "quarterdeck.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quarterdeck.selectorLabels" . | nindent 4 }}
